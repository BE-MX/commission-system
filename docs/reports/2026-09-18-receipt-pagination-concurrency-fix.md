# 回款分页并发核验修复（2026-09-18）

用户反馈张笑浅色库存0924已生成方舟回款但同步失败。发票540、回款654，未取得远端ID，错误为“分页重复且时间窗口核验失败”。

## 根因及修复

生产只读执行原窗口核验，369次GET后复现“回款扫描期间发生变化”；最后无过滤total为18347。原代码以全库总数作为冻结时间窗口的基准，并要求扫描结束全库总数不变；数分钟期间其他订单新回款也会阻止本单发送。

按[小满回款列表接口](https://open.xiaoman.cn/api-3478279)使用更新时间窗口。现在初始总数与扫描范围一致，仍严格校验逐窗数量、同秒边界、唯一ID与历史冻结根。完整基线后，用重叠一分钟到新截止时间的增量窗口双读；必须全量、唯一且两次相同，按ID合并并处理订单转入/转出和金额更新。最终核对合并后的全库数量和最新页时间，不允许遗漏增量截止后的同数量修改。

保留安全阻断：历史窗口缩小/变化、同秒超过100条、增量超过100条、不完整/重复/变化、截止后仍有更新或时钟倒退均不发送。错误保留具体核验原因，不再统一掩盖为窗口失败。金额、占额、凭证、租约、POST次数及回读逻辑未改动。

## 验证与交付

新增回归先复现原逻辑对其他订单新增及目标订单新增的误阻断；修复后78项回款协议、管理及存储测试通过。独立审查补充同数量晚更新风险并修复复验通过。增量约定与diff检查通过；全局约定仍有7项已有UI行数基线问题，未改基线。

代码提交7b2356c7，分支codex/receipt-pagination，基于生产c9351606。已通过统一入口完成办公室/北京生产发布，日志为MANAGED APPLICATION RELEASE COMPLETED、deferred=[]、schema157未变，两域健康检查正常。候选真实只读扫描372次查询完整通过，该订单远端回款0条；原单654重试成功，小满回款ID105794102302050，USD114.68，回读collect_status=1。原单不更换request_key，不另建本地回款。证据保留在主目录tmp/receipt-enable/中diagnose-window、pagination-tests、deploy-pagination和后续原单重试日志。

## 持久增量核验

用户补充要求避免每单全量扫描，新增私有回款索引 `backend/data/receipt-index/<API与应用标识摘要>.json`。首次完整核验成功后原子保存全部最小字段与核验水位；索引有格式版本、源标识和内容校验和，不保存token、附件链接、客户详情。目录不属静态挂载且被Git忽略；办公室目录ACL只允许SYSTEM、Administrators和服务维护账户，Linux文件0600。

每次使用索引仍联网核验：从上次水位前60秒到当前读取增量两次确认完整一致，按ID更新金额、财务状态和订单归属，再用最新页时间及全库唯一数复核，通常3次列表GET。服务重启或新进程直接复用磁盘索引。删除导致数量不匹配、同数量删增、索引损坏/源不匹配或增量超过100条等无法完整核验的情形，重新全量建立；远端网络/鉴权失败不返回缓存余额。未改POST、幂等、租约、凭证和本地占额。

并发写使用进程内锁、唯一临时文件和整份原子替换；跨进程旧的完整快照覆盖新的只造成重复增量查询，不会将新水位与旧记录混合。部署前核对办公室与小满时钟差约1.15秒，小于60秒重叠区间。

增量提交805d0a1b已通过统一入口部署办公室和北京，schema157不变；121项索引/回款/订单测试通过，独立审查通过，增量规则无违规，全局门禁仍为7项已有UI基线错误。办公室首次18352条、372次GET、310.08秒，后续3次GET、2.09秒；北京首次372次GET、185.12秒，后续3次GET、1.78秒。办公室新进程调用真实订单540余额接口，列表仅3次GET，总耗时7.98秒（含订单详情），总额114.68、已回款114.68、余额0.00，确认磁盘索引跨进程复用。当前分支codex/receipt-pagination尚未合并/推送GitHub；应保留工作树及主目录验证证据，后续主线集成需包含7b2356c7、805d0a1b。

## Tessie-KC-0913 只读排查

发票542、回款656（HK20260918-6a5f26c371564b2f）失败于订单一致性预检，尚未向小满创建回款。实时订单105794108809202客户94741803470032、币种USD均一致；方舟总额367.87，小满350.35，差额17.52等于surcharge_amount。订单同步明确不推Handling Fee（xiaoman_service._build_cost_list），回款order_snapshot却按含费total_amount严格比较，两个模块金额口径冲突。远端该订单回款查询为空；13:47:39首次失败、13:52:13重试仍失败。本轮只读查询，未改金额、未重试或生成回款。
