# 回款生产部署与验证（2026-09-18）

按用户要求将凭证集中办公室、单张上限设为 10MiB、新建小满回款直接有效，并继续生产验证。

## 已部署

- 生产办公室和北京均为 `aed61c435c617cdfc7d8736390f6c2c53f9d9c03`，来自 `codex/receipt-production-enable`，以实际生产 `dab19815` 为基点，未携带其后其他主线功能。应用与路由均走 `deploy/deploy.bat`，schema 仍为 156，无新增迁移。
- 新建小满回款发送 `collect_status=1`。同步状态与小满实际财务状态仍分别保存，须回读确认；不会将未知状态直接显示为有效。
- 两站 `/api/receipts` 统一进入办公室，截图仅方舟留存。北京经证书校验的 HTTPS 转发新加坡，再走办公室既有隧道。
- 每张凭证限制 10MiB；网关允许 11MiB 请求体以容纳 multipart 开销，关闭请求缓冲，读写超时 300 秒。

## 文件和上传

- 北京 4 份历史凭证复制至办公室；截至最后核验，办公室 28/28 份数据库凭证均大小与 SHA256 匹配，切换后再次归集新增复制数为 0。文件原件和 ZIP 备份保留。
- 办公室本机后端、SG 本地 `.work` 网关均完成 10MiB multipart 传输，返回未登录 403；12MiB 网关返回 413。此结果验证请求传输及入口限制，不代表登录后的图片内容校验或保存验收。
- `.cloud` 64KiB、256KiB 请求到达鉴权返回 403；1MiB/10MiB 测试多次超时。北京到新加坡 TCP 观测到严重重传、拥塞窗口仅 2，实测速率约数十 KB/s。不能宣称 `.cloud` 10MiB 端到端上传通过；当前仍有跨境链路性能阻碍。
- 用户真实小文件上传已有 HTTP 200，最新文件持续归集至办公室。浏览器工具连接失败，未完成本任务的登录态 UI 验收。

## 小满接口与分页

- 回款类型、字段、列表、详情及订单详情 GET 权限已实测可用。
- 小满列表第 23/24 页存在同更新时间的重复边界，原实现会拒绝余额核验。修复后先走正常分页；遇重复时完整重启为按更新时间缩小窗口，完整读取同秒边界，核对 ID 唯一性、行数、剩余总数及末尾总数。缺失或不稳定结果仍拒绝发送。
- 真实只读完整扫描通过：18,325 条、368 次查询。只读 GET 的瞬时网络错误最多重试一次；创建 POST 不做网络自动重试。

## 单笔真实创建结果

原单 `id=1`、`HK20260918-5adfac48aac64386` 首次在发送前因分页重复明确失败，未取得远端 ID。修复后通过现有 retry/deliver 状态机重试原单，先完整核验订单余额、凭证及重复单号，再发送并回读：`sync_status=synced`、`collect_status=1`、小满 ID `105794024173578`、无错误。实际创建权限与有效状态已验证，不需要额外开通本次接口。

最后状态为 1 张已同步、16 张 active/pending，自动意图 17 converted、2 draft、无 ready 阻塞。批量 `RECEIPT_SYNC_ENABLED=False` 保持不变；本轮仅授权测试的原单实际发送，未批量推送其余单据。

## 本地验证和交付

- 76 项回款协议、管理及部署路由回归通过；此前相关完整测试 261 通过、11 跳过。独立财务与路由审查无遗留阻断。
- `git diff --check`、覆盖 `dab19815` 起变更的增量约定检查通过；完整约定门禁仍报生产基点已有 13 项 UI 基线问题，未修改基线规避。
- `git_sweep.py --no-fetch` 已执行，属于本地快照。用户随后授权合并 main 并推送 origin；集成最新主线后复验，保留主目录验证证据，清理已合并任务工作树。
- 证据：主目录 `tmp/receipt-enable/` 中的部署、只读扫描、归集、单笔同步和上传日志。当前状态见 `docs/handoff.md`。
