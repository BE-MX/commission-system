# 全项目知识整理记录

## 范围与方法

用户明确要求整理整个项目。以当前主目录可见的Git已跟踪及未跟踪、非忽略Markdown为范围，逐文件读取正文，枚举标题、行数、相对Markdown链接与全文摘要；初次扫描239份、51,296行，未发现失效相对Markdown链接或全文完全重复文件。该扫描不校验外网链接、锚点、代码块内路径、裸文本路径及业务语义，也不读取忽略的凭据、运行数据或其它机器的记忆数据库。

重点人工核对根规范、README、文档导航、架构、API/数据库参考、运维/部署、记忆目录及交接记录；其余设计/实施计划按目录、标题和正文检索建立索引，没有宣称所有历史方案都与现行代码逐条一致。源码注册、Git历史、本轮既有部署证据是纠错依据，没有为整理文档重跑生产部署或操作业务数据。

## 已整理

| 问题 | 处理 |
| --- | --- |
| README与CLAUDE固定模块数不一致 | 删除失真的23/32计数，模块以源码注册和领域文档定位；补回款/公告入口 |
| README引导开发机迁移共享RDS | 改为隔离开发库；生产只走统一发布入口 |
| runbook仍描述pull、单服务停写、裸Alembic、逐文件SCP发布 | 合并为deploy/README唯一发布流程；保留专题历史维护条件并明确非当前升级指令 |
| 运维文档把111/104写成当前head | 改为模块历史最低依赖，不替代候选完整迁移链 |
| 旧版页面默认归因缓存 | 先核对发布status/revision、实际HEAD与线上入口制品，再处理客户端会话 |
| 交接文件混入超过1,400行历史状态 | 建立2026-09-17完整快照；后续交接仍有追加在旧条目末尾的记录，合并时保留完整正文避免遗漏 |
| 记忆规则在多处重复、指向忽略的旧.wolf文件 | 协议归AGENTS；本地同步方法归scripts/memory；既有领域笔记只建立索引，保留原内容 |
| 需求、设计、计划分散 | 新增全量文档目录，保留路径；设计/历史文档不自动成为生产事实 |

文档目录见[document-catalog.md](../document-catalog.md)。历史交接见[完整快照](../archive/handoff-2026-09-17.md)。运行事实与待办只维护[handoff.md](../handoff.md)，不在本报告另建第二份任务清单。

## 保留边界

未移动业务代码、需求原型、用户文件、历史审计证据、运行数据或部署候选。Git分支/worktree/stash已本地盘点；未合并、带改动及归属待核实的成果原样保留，不能凭“旧”或“已合并分支名”删除目录。未推送、合并、部署或调整生产权限。

Mem0本轮无可用连接器，未上传或宣称同步共享记忆；未安装新工具或历史回填。本仓库知识仍由上述权威文档维护。

## 验证

扫描脚本与前后清单保留于 `tmp/project-tidy/`，运行 `audit_docs.py` 可复核相对Markdown链接；目录索引是2026-09-17的盘点快照，新文档仍应加入docs/README的相关入口。备份保留本轮整理前文件，历史交接正文只改变链接基准，不删除原记录。

收尾结果：

- 最终枚举243份Markdown；目录索引收录其余242份。相对Markdown链接目标检查无失效项，未发现全文完全重复文件；不包含上述未覆盖的链接类型与语义验证。
- 历史交接快照保留原1,420行正文；2026-09-25合并时为保护其后追加的记录，当前交接仍保留完整正文。后续可按日期和主题核对后再精简。全仓未找到指向旧handoff章节的Markdown锚点引用。
- 独立agent只读审查规则、部署事实、历史保留及本地链接，未发现必须修复项。
- `git diff --check`通过。`scripts/check_conventions.py`退出1，报13项既有UI债务（6项按钮尺寸、7项行数基线漂移）；本轮未修改这些文件或重写基线，不宣称全量规则通过。
- `scripts/git_sweep.py --no-fetch`完成：main与本地origin/main引用一致；2条分支存在未合并提交，各worktree改动和stash均保留。未fetch，结果不代表远端实时状态。

纯文档整理未运行业务全量测试或构建，未执行commit、push或生产操作。扫描、备份和巡检记录保留在本地`tmp/project-tidy/`用于复核及恢复。

## 2026-09-18 提交范围复核

用户随后要求提交。文档改动转入独立codex/project-knowledge-tidy分支；未夹带其他任务的前端改动、脚本或小程序配置。提交版本的文档目录重新生成，排除主目录两份属于其他任务且尚未跟踪的文档：本分支共241份Markdown，索引收录其余240份，相对链接检查无失效项。上文243份是主目录整理时的盘点口径。原始备份与验证日志仍在主目录tmp/project-tidy中。此次只提交本地分支，不合并、推送或部署。

暂存差异检查另清除了历史快照中两行原有行尾空格，正文文字保留；此为上文逐字核对之后的格式修正。
