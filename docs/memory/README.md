# 项目稳定记忆

本目录只保存跨会话仍成立的架构决策、业务口径、已验证事实和上线红线；临时进度、原始日志、未确认方案、密钥及客户数据不得写入。

命名使用 `project_<domain>.md`，一个领域一个文件。决策改变时直接更新对应文件并写“最后核实”日期，不追加流水账；领域退役后删除文件，并由 Git 历史承担追溯。

当前文件：

- `project_customer.md`：统一客户经营主档、Agent 边界、数据库契约和切换红线。
