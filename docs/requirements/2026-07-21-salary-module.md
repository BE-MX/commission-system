# 薪资计算模块设计文档（salary）

- 日期：2026-07-21（2026-08-06 复核修订：架构+口径 8 项决策确认、数据模型微调、分期落地为 M1-M4）
- 状态：**设计已定稿，进入实现**（分支 `claude/salary-p1`）
- 材料来源：`C:\Users\windb\Downloads\薪资系统所需 材料`（8 份文件，逐一分析见 §2）
- 核心验收标准：**用系统复算 2026 年 3 月工资，引擎 9 列 66 人分位一致 + 手动 5 列以 3 月真值填入参与实发校验**（已知差异见 §2.5，按修正口径核对）

---

## 1. 结论先行

1. 工资表本质是一台「取数 + 公式 + 人工修正」的机器：底薪来自职级表、考勤来自钉钉导出、社保公积金来自代缴机构 Excel、绩效来自规则计算、少数列纯手工。目前全流程人工，**3 月表已发现 9 处以上人为错误（含一处资金风险级）**（§2.5），自动化价值明确。
2. 模块定位：新领域模块 `app/salary/`，做「月度工资批次」工作流——同步钉钉考勤 → 导入社保公积金 → 引擎计算 → 人工复核修正 → 锁定 → 导出与人工表同构的 Excel。**业务提成不进本模块**（现有提成域按订单回款独立发放，3 月工资表本身也不含提成列）。
3. 考勤通过钉钉开放接口拉取（平台 `app/dingtalk/client.py` 已有 token 基建，补考勤 API 封装即可），全勤奖、缺勤扣款、年假备注全部自动生成。
4. 规则全部参数化 + 版本化（职级表注明「2026/04/01-更新新版本为止」，规则本身就是带生效期的）。
5. 分三期：P1 核心复算引擎 + 导入导出；P2 绩效自动化 + 钉钉工资条；P3 晋升调级联动 + 个税。三期边界见 §8。

### 1.1 已确认决策清单（2026-08-06 亮哥拍板，代码实现按此执行）

**架构级 4 项**：

| # | 决策 | 落地位置 |
|---|------|----------|
| A1 | P1 验收颗粒度 = **引擎 9 列分位一致 + 手动 5 列以 3 月真值填入**参与实发校验 | §8 P1 出口标准、§6 计算引擎 |
| A2 | `salary_record` **仅 4 列拆 auto/manual/final** 三元组（奖励/绩效/其他款/补贴），其余引擎列单字段落 final，个税单字段纯手动 | §4 数据模型、§6 重算语义 |
| A3 | 66 人档案初始化 = **从 3 月工资表 + 社保/公积金 + users 表反导入**，产出异常清单交 HR 逐条修正 | §8 M1、独立脚本 `backend/scripts/salary/import_from_march.py` |
| A4 | 批次 confirmed 后修正 = **admin 解锁重出，前次导出打作废水印**（无补发批次概念） | §7.1 状态机 |

**HR 业务口径 4 项**：

| # | 决策 | 落地参数 |
|---|------|----------|
| B1 | 全勤员工应出天数基准 = **固定 31 天**（2 月/4 月同此） | `rule_param.full_month_days=31` |
| B2 | 月中调薪/转正底薪加权 = **固定 30 天**（契合 3 月陈佳乐 3775 数据），且**仅底薪加权**，工龄/绩效目标/全勤按月末档案取 | `rule_param.mid_month_weight_base=30`，代码注释显式说明「B1 与 B2 分离不是笔误」 |
| B3 | 病假折算 = **按扣减 30% 实现**；年假 = **不破全勤** | `rule_param.sick_pay_deduct_ratio=0.30`、`rule_param.annual_leave_breaks_attendance=false`，两参数都可切以留 HR 反悔口子 |
| B4 | 明细/汇总表职务列 = **单一真相源 `profile.position`，两表同源导出** | 消灭 §2.5 错误 6 |

其余原 §9 开放问题（编号 4/6/7/8/9/10/11/12）**不阻塞 P1**，兜底见 §8.3。

---

## 2. 材料分析总结

### 2.1 《2026年3月工资表.xls》（核心输出物，两张 sheet）

**Sheet2「工资表（明细）」是计算真相源**，23 列：

| 列 | 性质 | 来源 |
|----|------|------|
| 序号/入职时间/工号/姓名/部门/职位 | 固定信息 | 员工薪资档案 |
| 应出天数 | 规则 | 满月=31（当月自然日）；月中入职/实出<15 天=当月工作日数（3 月为 22） |
| 实出天数 | 规则 | 应出 −（事假时长/7.83）−（病假时长/7.83×折算比例），考勤来自钉钉 |
| 应发工资（底薪） | 规则+档案 | 职级表（业务 P/M、跟单 F/M）或手动定薪；月中调薪/转正按天加权（陈佳乐 3775 介于试用 3500 与转正 4000 之间，但按 31 天基数复算为 3774.19 对不上——精确加权口径待 HR 确认，见 §9.2） |
| 奖励 | 规则+手动 | 单项奖金（月度销售额阶梯，3 月全空） |
| 绩效 | 规则+手动 | 业务员/管理岗按绩效规则算，社媒运营另有规则（材料缺），个别岗位手动 |
| 工龄 | 规则 | 200 元/满一年，上限 2000；按工资月所在月份过周年即上调 |
| 全勤奖 | 规则 | 100 元；无事假、病假≤8h、无迟到/早退/漏打卡/旷工；**年假不影响全勤** |
| 小计（增项） | 公式 | 奖励+绩效+工龄+全勤 |
| 社保/公积金 | 导入 | 与当月社保、公积金明细「个人部分」逐人匹配 |
| 缺勤扣款 | 公式 | −底薪/应出天数×缺勤天数（已验证：谷振尧 10000/31×4=1290.32；王槐竹 6000/22×5=1363.64；李晓雨 5000/21.75×8.18=1880.46） |
| 其他款 | 手动 | 正负皆可（如刘德明 +1000） |
| 小计（减项） | 公式 | 社保+公积金+缺勤+其他款 |
| 补贴项 | 手动+规则 | 每月手动；**保底工资补足自动化**：实发 = max(计算实发, 保底 − 缺勤扣款)，差额进补贴项（已验证：张甜甜 699、隋晓茹 1470、凯丽比努尔 1488、刘也 1679 复算吻合；负例也自洽——徐瑞萍/张紫娟保底 2026/04 起生效，3 月未补） |
| 实发工资 | 公式 | round(底薪 + 增项小计 − 减项小计 + 补贴项)，四舍五入到元 |
| 个税/扣除个税实发 | 手动 | 3 月全员为 0（申报走税务系统，表内仅留位） |

**Sheet1「工资汇总表」在金额上是明细的投影**（发放/公示口径）：按大部门分组（后综部 39 人 / 业务部 27 人，与明细的细部门是多对一映射），列 = 序号/部门/姓名/职务/入职日期/中行银行卡号/请假时间/实发工资/备注。注意：**职务列两表大面积不一致**（明细 vs 汇总：刘琳琳 M6/M2、毕晓珍 M6/M5、夏新月 M3/M1、宋皓月 M5/业务、李宝珠 M6/INS开发、田雯 M4/业务、刘也 外贸业务员/社媒运营）——汇总表职务疑似另一套对外/滞后口径，真相源归属见 §9.5。其余：
- 「请假时间」列 = 自动文案：`本月年假X天，本年度剩余年假Y天`（试用期员工则写底薪约定）→ 需要**年假台账**；
- 「备注」列 = 自动文案：`扣社保XX元，公积金XX元。`；
- 各部门合计 + 总计（3 月总计 434,525，与明细表合计一致，已核对）。

### 2.2 《2026年3月社保&医保明细.xls》

两个参保主体各一张 sheet：**青岛丽丝发贸易有限公司**（50 人）、**鄄城莱莎发制品有限公司青岛分公司**（18 人）。逐人列出缴费基数与各险种金额。从数据反推费率：养老个人 8%/单位 16%，失业个人 0.3%/单位 0.7%，工伤单位 0.32%，医保单位 8%/个人 2%。**工资表只取「个人总计」列**（= 养老个人+失业个人+医保个人，如 4504 基数 → 463.91）。注意：丽丝发 sheet 表头写「工伤上升至 0.2%」但两主体数据实际均为 0.32%——**导入解析不要信表头文案，费率一律从数据列反推校验**。

关键结构事实：
- 社保名单 ⊋ 发薪名单：8 人（曹其宽、张传明、王语涵、周笑雨、龙永彬、曹光芹、常华、乔兰兰）只参保不在本工资表 → 导入需支持「非发薪参保人员」白名单。注意其中曹其宽、张传明在社保表标注为外贸部——**白名单按人（档案 payroll_included 标记）判定，不能按部门字段判定**；
- 反向也存在：新入职未参保（王槐竹社保 0）、部分员工公积金 0（牟亮亮、刘德明、姜妮妮）。

### 2.3 《26年3月公积金.xls》

同样两主体两 sheet。基数统一 2200，个人/单位各 5% = 110 元。工资表取「个人费用」列。

### 2.4 四张规则图 + 绩效规则 docx（规则库原文）

1. **薪资基本规则.png**（总纲思维导图）：即 §2.1 各列规则的原始出处。补充两条重要规则：
   - 病假计薪：单次 ≤10 个工作日按本人底薪 70% 计；>10 个工作日按员工患病前 12 个月平均工资（不含提成）计，不满 12 个月按实际月数平均；病假累计 ≤6 个月按本人工资 70%，>6 个月按 60%；
   - 考勤折天系数 7.83（小时/天）。
2. **外贸业务员分配资源晋升规则.png**（资源分配赛道）：P1~P10 底薪 3500→10000，对应季度月均业绩标准 3500→100000 与月均新签单数下限（6/6/6/5/5/5/4/4/4/4）；管理岗 M1~M6 标准工资 5000→10000（=底薪+20%绩效）、半年月均业绩 15000→60000、团队人数配置、团队提成 1.0%；单项奖金月度阶梯（入职年限 × 当月销售额档 → 300/500/1000/1500 元，需先完成对应业绩目标，满 4 周年不享受）；P 级每季度调级（执行节点 10 月 1 日、1 月 1 日），M 级半年一次。
3. **外贸业务员开发晋升规则.png**（自主开发赛道）：同构，数字不同——P1~P10 底薪 4000→12000，新签单数下限（4/4/4/3/3/3/2/2/2/2），团队提成 1.5%，单项奖金阶梯 200~1500 元。**同一员工归属哪个赛道由档案标记，赛道切换按「首单决定制」认定客户归属**（提成域概念，工资模块只需要记录赛道以选对职级表）。
4. **跟单晋升机制.png**：跟单 F1~F6 底薪 4000→6500（对应季度月均发货 $3W→$18W）；跟单提成为计件制（发货 $1K 以下 ¥10/单、以上 ¥50/单，自主谈单额外 +¥10）；晋升管理岗标准：季度发货订单 200 笔 或 季度自谈业绩 $6W 或 季度总业绩 $30W；跟单管理岗 M1~M3 底薪 5K/6K/8K（对应半年月均 $9W/$12W/$15W）+ 团队提成 0.1%。
5. **外贸业务员绩效规则.docx**：
   - 满绩效：基础业务员 200 元；管理岗 = 标准工资 × 20%（与 3 月数据吻合：M6 刘琳琳满绩效 2000、实得 1950）；
   - 转正未满一年（自转正次月起 12 个月）：得分 = 50%新成交数量完成率 + 30%业绩完成率 + 20%好评数量；
   - 转正一年以上：30% / 50% / 20%；
   - M 岗：30%团队新成交数量率 + 30%团队业绩完成率 + 20%团队好评 + 20%个人业绩完成率（团队口径均不含队长，分母为成员对应 P 级要求之和；团队好评=总好评数/(团队人数×2)；个人业绩完成率对标 M 级半年月均业绩）；
   - 绩效金额 = 满绩效 × 综合得分。

### 2.5 人工流程的错误实证（自动化依据）

3 月表交叉核对发现 **9 处以上**真实错误，全部属于「复制粘贴/誊抄/错行」类，系统化后此类错误结构性消失：

1. **汇总表社保备注三人错位**：孙正华备注写成 553.32（实为吕德洋的）、谷振尧写成 550.54（实为孙正华的）、吕德洋写成 463.91（实为谷振尧的）——备注列相对真值错了一行，形成三人轮转；
2. **银行卡号复制错误（资金风险级）**：江倩倩的银行卡号与刘德明完全相同（6217886******9734）——若按此发放，钱会打错人；
3. **身份证号复制错误**：公积金表中刘也的身份证号与姜婷相同（社保表中可见刘也真实证号，两处不一致）；
4. **工号重复**：刘德明/江倩倩同为 293，张桂云/陈佳乐同为 319；另有吕德洋 3 与刘美美 003 归一化后撞号；
5. **入职日期两表冲突**：江倩倩 2025.2.11（明细）vs 2025/02/21（汇总）；尹德魁 44956 vs 44957；张桂云 2025.12.15（明细，疑从陈佳乐行抄来）vs 2025/12/01（汇总）；
6. **职务列两表大面积不一致**：至少 7 人（见 §2.1）。

对应系统对策：汇总表备注/请假文案由明细自动生成（消灭 1）；银行卡号、身份证号、工号唯一性校验（消灭 2、3、4）；固定信息单一来源=薪资档案，两张 sheet 同源导出（消灭 5、6）。

**对验收口径的影响**：P1「复算 2026-03 逐人逐列一致」需附带一份「已知差异清单」——上述错误行按修正后口径核对，职务列以 §9.5 确认的真相源为准。

---

## 3. 模块边界与总体架构

### 3.1 边界（明确不做什么）

- **不做业务提成**：提成按订单回款发放，归现有提成域；工资表 23 列中无提成列，维持现状分轨；
- **不做个税计算引擎**（P1）：表内个税列保留手动录入，默认 0，申报仍走自然人电子税务局（P3 再评估累计预扣法，见 §8）；
- **不做晋升自动判定**（P1）：职级是档案字段 + 调级记录，调级动作人工（P3 联动业绩数据自动预警，见 §8）；
- **不改钉钉考勤规则本身**：钉钉是考勤唯一真相源，本模块只读。

### 3.2 领域模块结构（遵守宪法硬约定 1/2/4）

```
backend/app/salary/
├── router.py            # 全部端点，app/routers.py 注册，权限 Depends + ok() 信封
├── models.py            # 8 张表（见 §4）
├── schemas.py
└── service/
    ├── __init__.py      # facade
    ├── period_service.py      # 批次工作流/状态机
    ├── attendance_service.py  # 钉钉考勤同步与折算
    ├── calc_service.py        # 计算引擎（纯函数核心，必须可单测）
    ├── import_service.py      # 社保/公积金 Excel 导入与对账
    ├── export_service.py      # 汇总表+明细表 Excel 导出
    ├── profile_service.py     # 薪资档案/职级/调薪记录
    └── rule_service.py        # 规则参数（版本化）读取
```

前端 `frontend/src/views/salary/`（大页拆 composables），API 模块 `frontend/src/api/salary.js` 走 `clients.js` 登记。

---

## 4. 数据模型（Alembic 一次迁移，表名前缀 salary_）

```
salary_employee_profile        员工薪资档案（1 人 1 行，工资口径的主数据）
  ├─ user_id FK→users(可空)    平台账号关联（可空：质检等无平台账号员工）
  ├─ emp_no, name, id_card_no(加密), bank_card_no(加密), bank_name
  ├─ hire_date, regular_date(转正日), leave_date, status(在职/离职)
  ├─ dept_detail(明细部门), dept_group(汇总大部门)
  ├─ position(职务), grade_scheme(资源分配/自主开发/跟单/管理/无), grade_code(P1..M6/F1..)
  ├─ base_salary_override      手动定薪（非职级岗位）
  ├─ probation_salary, probation_note   试用期底薪与备注文案
  ├─ guaranteed_salary, guaranteed_from, guaranteed_to   保底工资及生效区间
  ├─ insurance_entity(参保主体), payroll_included(是否发薪), fund_included
  ├─ dingtalk_userid, mobile
  ├─ id_card_hash, bank_card_hash   # HMAC-SHA256(明文, 服务端密钥)，密钥走 Settings/.env
  └─ 唯一性约束建在 hash 列上（AES 随机 IV 下密文列无法做唯一索引，也无法与导入的
     明文身份证 JOIN——导入匹配、查重全部走 hash 列；对应 §2.5 错误 2/3/4，emp_no 同样唯一）

salary_grade_table             职级薪级表（版本化）
  ├─ scheme(赛道/序列), grade_code, base_salary, perf_full(满绩效), std_salary(管理岗标准工资)
  ├─ perf_target_monthly(月均业绩标准), new_sign_min(月均新签下限), team_rate(团队提成参考值)
  └─ effective_from, effective_to        # 「2026/04/01-更新新版本为止」的落地

salary_rule_param              规则参数（版本化 KV）：day_hours=7.83、full_month_days=31、
                               attendance_bonus=100、seniority_step=200、seniority_cap=2000、
                               sick_pay_ratio=0.7、sick_pay_ratio_6m=0.6、社保公积金费率等

salary_period                  月度批次：year_month(unique)、status、workday_count(当月工作日数)、
                               natural_days、param_snapshot(JSON,计算时冻结参数)、锁定人/时间

salary_attendance              考勤汇总（批次×员工）：应出、实出、事假h、病假h、年假天、年假剩余、
                               迟到/早退/漏打卡/旷工次数、全勤flag、sync_source(钉钉/手动)、raw JSON

salary_insurance_import        社保导入行：entity、id_card_no、name、base、personal_total、
                               company_total、明细各险种、match_status(已匹配/非发薪/未识别)
salary_fund_import             公积金导入行（结构同上，字段少）

salary_record                  工资明细行（批次×员工，工资表 23 列全量落库）
  ├─ 每个金额列都拆 auto_value + manual_value + final_value 三元组：
  │    引擎写 auto，人工改 manual，final=coalesce(manual,auto) → 重算不冲掉人工修正，且留痕
  ├─ 档案快照列：emp_no/name/dept_detail/dept_group/position/bank_card_no(加密)/bank_name
  │    ——批次 confirmed 时冻结。银行盘、汇总表、重导出一律读快照而非活档案，
  │    否则锁定后员工换卡会污染历史批次（正是要消灭的错误 2 的系统版）
  ├─ remark_summary(汇总表备注,自动生成), leave_remark(请假时间文案,自动生成)
  ├─ row_version(乐观锁，复核期多人编辑冲突检测)
  └─ audit: calculated_at, modified_by, modify_reason

唯一约束：salary_record 与 salary_attendance 均为 (period_id, employee_id) 唯一。

salary_change_log              调薪/调级/转正记录：employee_id、change_type、生效日、
                               旧值/新值(JSON)——月中转正加权计薪的依据（陈佳乐 3775 案例）
```

敏感字段（身份证、银行卡）AES 加密存储、界面默认脱敏、导出时按权限明文。

### 4.1 数据模型微调（2026-08-06 复核后调整）

相对上文原设计，实现前落地这 3 处修改：

1. **`dept_group` 从档案挪出成独立映射表**（原设计 §4 档案里的 `dept_group` 字段删除）：
   ```
   salary_dept_mapping: dept_detail(UK) → dept_group
   ```
   明细部门 → 大部门是配置关系，不该冗余到 66 人档案。HR 在规则页维护，映射调整不用同步档案。

2. **`salary_record` 三元组仅 4 列**（其余列砍成单字段，表宽度降至原设计 60%）：
   ```
   引擎列（直接落 final，不留 auto/manual 位）：
     base_salary, seniority_pay, attendance_bonus,
     social_insurance, housing_fund, absence_deduction,
     add_subtotal, deduct_subtotal, net_salary
   三元组列（auto / manual / final，manual != NULL 时表示人工覆盖）：
     bonus_amount, performance_amount, other_amount, subsidy_amount
   纯手动列（单字段）：
     income_tax_amount
   ```
   final = coalesce(manual, auto)；异常面板判定「auto 已变化但被 manual 覆盖」= `manual IS NOT NULL AND manual != auto`。

3. **批次状态机加乐观锁**：`salary_period.status_version`，状态跃迁前校验，避免「重算中」和「锁定」并发碰撞（`salary_record.row_version` 只管行级手动编辑冲突，状态跃迁走批次级）。

## 5. 钉钉考勤集成设计

平台 `app/dingtalk/client.py` 已有 access_token 与通用 get/post，本模块在 `attendance_service.py` 内新增封装（不改 dingtalk 域，只调用其 client——与 approval.py 同层的用法）。

### 5.1 取数路径（三路互补）

| 数据 | 钉钉接口 | 用途 |
|------|----------|------|
| 月度考勤报表列值 | `topapi/attendance/getattcolumns` + `topapi/attendance/getcolumnval` | 与 HR 现在手工导出的「钉钉月度考勤报表」同源，直接拿出勤天数、事假/病假/年假时长等聚合列，是主路径 |
| 打卡结果 | `attendance/list`（≤50 人/批，7 天窗口分页） | 迟到/早退/漏打卡/旷工明细次数 → 全勤奖判定 |
| 请假明细/额度 | `topapi/attendance/getleavestatus`（20 人/批）+ `vacation/type/list` + `vacation/quota/list` | 假期按类型拆分（事假/病假/年假）、年假余额 → 汇总表「请假时间」文案 |

员工映射：档案存 `dingtalk_userid`，初始化用已有 `get_userid_by_mobile(mobile)` 批量回填，匹配不上的进异常清单人工绑定。

### 5.2 折算与判定（引擎输入）

- 事假折天 = 事假小时 / 7.83；病假折天 = 病假小时 / 7.83 ×（1 − 病假计薪比例）——规则原文「相对于的比例」有歧义（×30% 扣减 vs ×70%），3 月无病假样本可验证，**方向待确认（§9.3），P1 按扣减 30% 实现 + 支持手动改**；
- 全勤 = 无事假 且 病假 ≤8h 且 迟到/早退/漏打卡/旷工均为 0。**「年假不破全勤」是从数据反推的**（王京花年假 5.5 天仍得全勤 100），规则图该句文字残缺，待 HR 确认（§9.3）；反向佐证：3 月有 5 人 31/31 全勤却为 0，只能用钉钉迟到/漏打卡明细解释，说明打卡明细路径必须接；
- 应出天数 = 满月员工取当月基准天数（3 月=31，参数 full_month_days，2 月口径待确认 §9.1）；当月入/离职或实出 <15 天 → 取当月工作日数（batch 上存 workday_count）；
- 同步为**拉取式**：批次页点「同步考勤」触发（也支持每月 1 日 scheduler 预拉上月），拉完先落 `salary_attendance` 供人工核对，不直接进计算；
- **历史数据依赖**：P1 验收要复算 2026-03，前提是钉钉仍可回拉 3 月考勤明细（钉钉考勤数据留存有时限），实现第一步先验证可回拉范围，拉不到则 3 月考勤用人工表数值代录完成复算。

### 5.3 失败与兜底

接口全部走 savepoint 单人隔离（宪法硬约定 6）：某员工拉取失败不阻塞整批，标记 sync_failed 进异常清单；支持单人重拉、全量重拉、手工录入覆盖（sync_source=手动）。

## 6. 计算引擎

`calc_service.py` 核心为**纯函数**：`calc_record(profile, grade, attendance, insurance, fund, params, manual_overrides) -> RecordDraft`，不碰 session，管钱逻辑 100% 可单测（DoD 硬要求）。

```
底薪        = 档案定薪 or 职级表底薪；月中调薪/转正 → 按 change_log 生效日按天加权
              （加权天数基数 31 vs 30 vs 工作日待确认 §9.2，3 月陈佳乐 3775 两种口径差 1 元）
奖励        = 单项奖金（P1 手动；P2 按销售额阶梯规则算，见 §8）
绩效        = P1: 手动录入金额（引擎只给参考=满绩效）
              P2: 满绩效 ×（权重×完成率），权重按转正年限/M 岗自动选
工龄工资    = min(200 × 周年数, 2000)，周年数按「工资月内过入职纪念月即+1」
全勤奖      = 考勤判定 ? 100 : 0
社保/公积金 = 导入表按身份证号匹配的个人部分（匹配不到→0 并进异常清单）
缺勤扣款    = 底薪 / 应出天数 × (应出 − 实出)
补贴项      = 手动 + 保底自动补足：若 计算实发 < 保底 − 缺勤扣款，补差额（生效区间内）
实发        = round(底薪 + 奖励 + 绩效 + 工龄 + 全勤 − 社保 − 公积金 − 缺勤扣款 + 其他款 + 补贴)
              其中「其他款」是带符号净额（可正可负，如刘德明 +1000 使减项小计变为 +283.12），
              社保/公积金/缺勤扣款恒为非负、按减项处理
```

重算语义：任何输入变化（考勤重拉、导入更新、参数修正）→ 重算只更新 auto_value，人工 manual_value 保留并高亮「auto 已变化但被人工覆盖」的行，复核者决定是否放弃覆盖。

**负数实发拦截**：低底薪 + 大额缺勤 + 社保可算出负数（3 月最低已到 1114 元）。final 实发 < 0 的行禁止进入 confirmed，强制进异常清单人工决策（清零挂账下月 / 其他款冲抵 / 照发负数需 admin 显式确认），银行盘生成时二次校验金额 > 0。

## 7. 工作流、页面与权限

### 7.1 批次状态机

```
draft → attendance_synced → imported → calculated → reviewing → confirmed(锁定) → exported/paid
```

- confirmed 前可任意重算；confirmed 时将档案字段（含银行卡号）快照进 salary_record，此后全表只读，修正需 admin 解锁并留痕；
- 复核是多人协作（制表/审核/复核三签），手动列编辑走 row_version 乐观锁：提交时版本不符 → 提示「该行已被他人修改」并展示对方改动，不做静默覆盖；
- 每步完成度在批次页用步骤条呈现，未就绪项（考勤未同步 N 人、社保未匹配 M 人）直接显示在步骤上，反馈引导行动。

### 7.2 页面（navigation.js 各加一条 entry，新分组「薪资管理」）

| 页面 | 内容 | 标杆 |
|------|------|------|
| 工资批次列表 | 月份、状态、总额、操作 | ExpoLeads 编排（useListPage） |
| 批次工作台（核心页） | 步骤条 + 明细表格（23 列，手动列行内编辑）+ 异常面板 + 导入对话框（上传用 `components/AppUpload`）+ 导出按钮 | 大页拆 `composables/useSalaryPeriod.js` |
| 员工薪资档案 | 档案 CRUD、职级/赛道、保底、参保主体、钉钉绑定、调薪记录 | DictManagement 标杆 |
| 规则配置 | 职级薪级表（按版本）、规则参数、大部门映射 | DictManagement 标杆 |

异常面板是体验核心（不要让用户思考）：未匹配社保、考勤失败、卡号/身份证重复、保底触发、月中调薪加权、auto 被人工覆盖但 auto 已变 —— 全部聚合成待办清单，点击定位到行。

### 7.3 导出（核心输出物）

openpyxl 生成与人工表同构的 .xlsx：Sheet1 汇总表（按大部门分组、组内重排序号、合计/总计行、自动备注文案、制表/审核/复核脚注）+ Sheet2 明细表（23 列 + 合计行）。另出银行代发盘（姓名/卡号/金额 CSV）作为可选导出。**confirmed 批次的导出一律读 salary_record 快照，不回查活档案**。

### 7.4 权限（宪法硬约定 3/8）

- `salary:read` / `salary:write` / `salary:admin` 三档起步（admin = 锁定/解锁/规则配置/明文导出）；
- 薪资是全平台最敏感数据：navigation entry 用 permission 声明，仅 HR 与管理层角色分配；接口层每端点 `Depends(require_permission)`；
- 员工个人工资条自助查询（self 数据范围）属 P2，届时按数据范围权限规范登记 kind=data。

## 8. 分期计划

| 期 | 内容 | 出口标准 |
|----|------|----------|
| **P1 核心复算** | 档案/职级表/参数、钉钉考勤同步、社保公积金导入对账、计算引擎（绩效手动）、批次工作流、双表导出 | **复算 2026-03 全月：66 人 × 9 引擎列 = 594 个数分位一致 + 5 手动列以 3 月真值填入参与实发校验**（附「已知差异清单」：§2.5 的 9 处错误按修正后口径，职务列按 B4 单一真相源），pytest 覆盖引擎全部分支 |
| **P2 绩效与触达** | 业务员/管理岗绩效公式自动化（新成交数/业绩/好评三输入，先手工填完成率后接数据源）、单项奖金规则化、钉钉工资条推送（复用 work_notify）、员工工资条自助查询 | 绩效自动值与人工核算一致；工资条送达率 100% |
| **P3 联动与深化** | 晋升调级预警（对接 OKKI/发票域业绩数据）、个税累计预扣法评估、年假额度台账与钉钉额度同步 | 另立需求文档 |

### 8.1 P1 里程碑拆解（M1~M4，约 9 周）

```
M1 档案 + 规则骨架(2 周)
├─ Alembic 一次迁移 9 张表(比原设计 +1: salary_dept_mapping)
├─ 档案反导入脚本 backend/scripts/salary/import_from_march.py
│   3 月工资表 + 社保/公积金表 + users 表 → salary_employee_profile
│   产出异常清单.xlsx: 工号重复/银行卡重复/身份证不一致/入职日期两表冲突/参保白名单
│   交 HR 逐条批注,人工确认后落库(消灭 §2.5 的 6 类错误源头)
├─ 职级薪级表种子(4 张规则图 P1~P10 / M1~M6 / F1~F6 / 跟单 M1~M3 全量录入)
├─ 规则参数种子(rule_param KV,含 B1~B3 决策落地)
├─ 权限码 3 个(salary:read/write/admin) seed_role_permissions upsert
├─ dept_mapping 表种子(3 月表已见的明细部门 → 后综部/业务部)
└─ 前端: 员工档案页(DictManagement 标杆) + 规则配置页

M2 考勤 + 导入(2 周)
├─ 钉钉考勤三路封装(attendance_service):
│   getattcolumns/getcolumnval(主路径,月度报表列值)
│   attendance/list(打卡明细,判定全勤)
│   getleavestatus + vacation/quota/list(请假拆分 + 年假余额)
│   第一步先跑通"能拉 3 月数据"验证,拉不到走人工录入兜底
├─ 社保 Excel 解析:两 sheet(丽丝发/鄄城分公司)反推费率校验 + 明文身份证走 HMAC hash 匹配
├─ 公积金 Excel 解析:同结构
├─ 异常面板 v1:未匹配社保/参保白名单/迟到明细/钉钉未绑定 逐人可点定位
└─ 前端: 批次工作台步骤条 + 考勤子页 + 导入子页 + AppUpload

M3 计算引擎(3 周,最难段)
├─ 纯函数 calc_record(profile,grade,attendance,insurance,fund,params,manual) -> RecordDraft
│   9 列引擎产出:base/seniority/attendance_bonus/social/fund/absence/add_sub/deduct_sub/net
│   分段函数:实出<15 天判定 → 切换基准 → 重算(两阶段显式,不做循环依赖)
│   月中调薪加权只对 base_salary(B2),其余按月末档案取,代码注释锁死不允许维护者「修正」
│   病假 30% 扣减(B3);年假不破全勤(B3)
│   负数实发拦截:final net_salary<0 强制入异常清单,银行盘二次校验
├─ pytest 覆盖 15+ 分支:
│   满月/月中入职/月中转正/月中调薪/月中转正+调薪叠加/
│   保底触发/纯缺勤/事假+病假/年假不破全勤/参保白名单/
│   公积金 0/社保未匹配/其他款±/负数实发/银行卡撞号
│   每分支用 3 月真人数据做用例(66 人样本充足)
├─ 重算语义:auto 变化但 manual 覆盖 → 高亮标记 diff,复核者决定是否放弃覆盖
└─ 前端: 明细表 23 列 + 手动 4 列行内编辑 + row_version 冲突提示

M4 复核工作流 + 导出 + 3 月验收(2 周)
├─ 状态机 draft → attendance_synced → imported → calculated → reviewing → confirmed
│   confirmed 时冻结档案快照到 salary_record(消灭 §2.5 错误 2/3/5/6 的根本机制)
│   status_version 乐观锁(避免"重算中"和"锁定"并发碰撞)
├─ admin 解锁流程(A4):前次导出打「作废」水印 + 审计留痕 + 重新出具
├─ openpyxl 导出双 sheet:
│   Sheet1 汇总表(按 dept_group 分组,组内重排序号,自动备注/请假文案,合计/总计)
│   Sheet2 明细表(23 列 + 合计行)
│   银行代发盘(CSV):两参保主体默认拆两盘(可切换单盘)
├─ 3 月复算验收:66 人 × 9 引擎列 = 594 个数分位一致
│   手动 5 列以 3 月真值填入,参与实发校验
│   已知差异清单挂 docs/salary-march-2026-diff.md,§2.5 的 9 处按修正口径核对
└─ 文档同步 + auto-memory project_salary_module.md
```

### 8.2 数据模型微调（相对 §4 原稿）

三处调整,原 §4 表结构相应替换:

1. **`dept_group` 挪出档案 → 独立映射表 `salary_dept_mapping`**（`dept_detail` UK → `dept_group`）。明细部门 → 大部门是配置关系,不该冗余到 66 行档案,HR 调整走规则页而非批改档案。
2. **`salary_record` 三元组仅 4 列**（B 依 A2 决策）：
   - **拆三元组**(`*_auto`/`*_manual`/`*_final`)：`bonus_amount` / `performance_amount` / `other_amount` / `subsidy_amount`
   - **单字段直接落 final**：`base_salary` / `seniority_pay` / `attendance_bonus` / `social_insurance` / `housing_fund` / `absence_deduction` / `add_subtotal` / `deduct_subtotal` / `net_salary`
   - **纯手动**：`income_tax_amount`
3. **`salary_period` 加乐观锁**：`status_version INT DEFAULT 0`,状态跃迁前校验,防重算/锁定并发碰撞。

### 8.3 剩余开放问题的兜底方案（P1 不阻塞）

| 原 §9 | 问题 | P1 兜底 |
|---|---|---|
| 4 | 绩效数据源 | 手动录入,P2 前调研 |
| 6 | 社媒运营绩效规则 | 手动录入,P2 前问 HR 补 |
| 7 | 跟单绩效规则 + 计件提成 | 手动录入 + 跟单提成暂不进本模块(HR 确认要进则 P2 加列) |
| 8 | 个税 | 全员默认 0,永久保留手动列位 |
| 9 | 姜妮妮/刘德明特殊人员 | 反导入产出异常清单交 HR 确认档案标记 |
| 10 | 李晓雨 21.75 应出 | 反导入按 3 月真值填入,规则待 HR 说明后回填参数 |
| 11 | 大部门映射维护 | M1 就有 dept_mapping 表,HR 直接配 |
| 12 | 两参保主体银行盘 | 默认拆两盘导出(可切换单盘) |

## 9. 开放问题（已决 8 项，剩余走 P2 或 P1 手动兜底见 §8.3）

**2026-08-06 亮哥拍板 8 项**（架构 A1~A4 + HR 口径 B1~B4）：

- **A1 P1 验收颗粒度**：引擎 9 列到分位一致 + 手动 5 列以 3 月真值填入参与实发校验
- **A2 三元组粒度**：仅 4 列拆(奖励/绩效/其他款/补贴),其余单字段
- **A3 档案初始化**：从 3 月工资表 + 社保/公积金表 + users 表反导入(M1 里程碑首任务)
- **A4 锁定后修正**：admin 解锁重出,前次导出作废(状态机加返工循环)
- **B1 应出天数基准**：固定 31 天(rule_param.full_month_days=31)
- **B2 月中调薪/转正加权**：固定 30 天(rule_param.mid_month_weight_base=30,与 B1 参数分离)
- **B3 病假 + 年假**：病假扣减 30% 实现,年假不破全勤;规则参数留可切
- **B4 职务列**：单一真相源(salary_employee_profile.position),两 sheet 同源导出

以下为**原 §9 未决项**，兜底方案见 §8.3：

1. **绩效三项数据源**（P2 前必答）：新成交数量、业绩完成率、好评数量目前从哪里统计（OKKI/阿里后台/人工台账）？决定 P2 是接口对接还是录入表单。
2. **社媒运营绩效规则缺失**（P2 前必答）：3 月绩效 60~960 元，明显不是业务员 200 满绩效体系，规则文档需补充。
3. **跟单绩效规则缺失**（P2 前必答）：跟单绩效 500/800/1000，规则未在材料中；跟单计件提成（¥10/¥50/自谈+10）是否也应进工资表（张桂云/陈佳乐补贴项同为 3710.7 疑似与此相关）？
4. **个税**（P3 前必答）：刘德明、牟亮亮档位按累计预扣应有税额但表内为 0——确认个税确实全程走税务系统、工资表永远只留位？
5. **特殊人员**（姜妮妮、刘德明，部门为「-」、无公积金/工龄/全勤）：这类档案的计薪规则是否就是「只算底薪±手动项」？M1 反导入产出异常清单，交 HR 逐条确认档案标记。
6. **李晓雨应出 21.75** 的口径无法从材料复原（22 工作日 − 0.25？），需 HR 说明，影响非满月规则的精确实现。M1 反导入按 3 月真值填入。
7. **汇总表大部门映射**：后综部/业务部的划分标准由谁维护（目前按明细部门映射可覆盖，需确认归属无歧义）。M1 落 dept_mapping 表由 HR 维护。
8. **发薪主体**：两参保主体是否对应两套发薪账户/两张银行盘，还是统一发放？影响银行代发盘拆分。M4 默认拆两盘（低成本反悔）。

## 10. 对现有系统的影响

- 新增独立领域模块，不触碰共享层与提成域冻结代码；
- dingtalk 域零改动（只消费其 client）；
- 新增权限码 3 个（seed upsert 自动刷新）；
- 一次 Alembic 迁移 8 张表，全新表无老数据兼容问题，但仍需满足「老代码+新 schema」过渡期兼容（纯新增，天然满足）；
- 文档三处同步（api-reference / database / auto-memory project_salary.md）在实现期完成。
