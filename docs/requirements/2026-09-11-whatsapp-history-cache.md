# WhatsApp 1.5.2 历史复用

包含 1.5.1 能力字段修复。首次生成自动采集；同一聊天再次生成优先复用页面内缓存，在底部时不滚动，保留早期消息并追加新消息。不在底部则只向下补齐；可见内容修改、删除末条或无法可靠衔接时重新采集。切换聊天、整体消息区替换、刷新页面清除缓存；不写浏览器存储。

只减少聊天采集，不复用旧草稿；每次仍将合并后的上下文交给 Agent 重新生成。当前 DOM 以外的历史编辑无法实时检测，网页历史完整性边界沿用 1.5.0。

类型检查、构建、230 项单元测试通过，覆盖120条复用+第121条追加、删除末条（底部/中间起点）、编辑、清缓存与容量限制。浏览器合成页面验证100条采集后重新生成不滚动；真实 WhatsApp 待实机验收。

安装：替换扩展文件并重新加载，确认版本1.5.2，刷新 WhatsApp Web。第一次会重新采集，此后的同聊天生成复用缓存。不需要更新后端。本轮未合并推送。

SHA-256: bce2bbf2299bd5e0a36afaf364dcfc5111b68914fde2300c79de5337bb296bf2

最终浏览器回归18/18通过，独立审查闭环。约定检查仍由未修改的资产模块两项既有UI基线问题阻挡；本次diff空白检查通过。
