# 话术事实资料与产品规格查询（1.6.5）

## 实际证据

只读核对共享后台最近成功话术请求的元数据，source_revisions仅有文档350/351/398三份约束，未包含341《发质与采购来源 FAQ》。生成预设系统提示为空。未读取聊天正文或模型输出日志，未修改生产数据。

复核发布FAQ：第5节明确不使用强酸浴快速剥离毛发；第6节明确不依赖厚重硅油涂层制造暂时柔顺，染后使用不含硅油滋养处理。不能扩大成任何阶段绝无酸或硅油的绝对保证。仓库21项绑定均通过当前权限、发布修订和哈希核验。

用新加载链路只读检索，实际命中341的5/6/7章节及三个必需约束。产品目录实际查询14英寸发帘，未匹配到记录；相关目录长度为16/18/20/22/24，结果未截断。该结论仅适用于当前启用目录，不等于不销售14英寸，也不能确认14英寸就是20g。

## 修改

默认从仓库根目录锚定的config/whatsapp-reply-phase12.json加载审核过的绑定，随部署更新，不再依赖手动复制整份环境JSON。WHATSAPP_REPLY_SOURCE_BINDINGS按(document_id,section_index)覆盖文件条目，明确blocked覆盖仍有效；自定义环境可设置WHATSAPP_REPLY_SOURCE_PROFILE为空以只使用环境绑定，或指定自有配置文件。文件缺失/非法格式显式报配置错误，合并配置进入请求签名和缓存复核。既有ACL、修订、哈希及对外用途约束保留，不自动刷新知识哈希。

生成前按当前会话的发帘产品族和明确长度查询启用的okki_products，仅投影model/size/unit；最多20条规格、30种概览长度，超限标记truncated，明确长度查询独立于概览上限。不查询价格、库存、订单或客户数据。需实时invoice:read、invoice:write或invoice_price:read之一（或super_admin），生成前后与缓存返回均重验。无权限或目录不可用如实标记，仍可回答取得的FAQ。

查询结果随conversation.product_catalog进入同一次生成调用。提示要求先使用已查事实回答、区分具体型号，不反问客户数字出处；禁止承诺没有实际任务支撑的“稍后找团队核实再跟进”。扩展面板显示目录已查到/未匹配/无权限/不可用状态。不引入输出语义拦截或额外模型调用。本版包含上一轮的1.6.4结果恢复改动。

## 验证与生效

后端来源/目录/生成/分段/服务/记忆/状态121项通过；补权限撤销、缓存权限和非法配置后42项专项通过；明确长度不受概览上限影响专项6项通过。扩展构建打包及283单测通过。独立审查发现的权限和配置边界均已修复。真实数据库仅只读核验；未调用生产模型、未向客户试发，不能将输入证据完备等同于最终生成质量已实测。

需部署本次后端和随版本config目录，安装1.6.5扩展并刷新WhatsApp。普通默认环境无需再手工应用之前的绑定片段；显式自定义覆盖仍须遵守上面的配置优先级。无数据库迁移。用户已授权合并推送；本轮不部署生产。

浏览器完整回归首轮27通过、1项询盘截图因CDP元素尚未布局失败；该项独立连续3次通过，未修改断言。约定检查保留素材库small按钮和行数基线两项既有失败；diff检查通过，Git巡检已完成。
