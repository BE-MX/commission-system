# 原型验证记录

2026-09-17，使用本机Chrome及Playwright，仅访问本地静态原型，没有调用业务API或数据库。

- 7种场景验证通过：日常冲刺、无团队达标、全部超额、并列第一、同率金额决胜、零新签、更新中断。
- 校验8组卡片、总目标113、每场景总完成数、第一团队数量与名称；嘉树在所有场景均不入第一。
- 屏幕正文不出现奖金字样、888或货币符号；同率金额仅用于演示决胜。
- 卡片和总览无内容溢出，底部规则在频道栏上方；日常与并列场景截图已人工查看。
- 1920×1080、1366×768、3840×2160、390×844的舞台均在视口范围内。手机为等比预览，非独立移动界面。
- 设计说明打开、Esc关闭及焦点归还通过；沉浸模式与Esc退出通过。
- 系统减少动态模式下无持续动画；无页面脚本异常、失败请求、缺失队徽。
- 原型沿用仓库tokens.css与既有队徽，保留仓库相对目录即可通过本地文件或静态服务器打开。

执行证据：`tmp/september-prototype/check.cjs`及`check-results.json`。截图为本目录`preview.png`，并列状态证据在`tmp/september-prototype/tie.png`。

工程检查：`python scripts/check_conventions.py`被当前基点已有的10项UI债务门禁阻断（旧small按钮及4个行数基线不匹配），均为未修改的frontend/src文件；不能记为全仓检查通过。本轮仅新增需求文档、静态原型及交接说明，不修改生产前端，无需执行生产构建。另独立执行增量规则函数检查本次差异，结果为空（无违规）；git diff --check通过。`git_sweep.py --no-fetch`已完成，结果是本地远端引用快照，不代表刚从远端获取的状态；没有执行push、merge或部署。

动效标准核对：原型不含持续动画、入场动效或动画计数；交互立即更新。按钮具备按压反馈、可见键盘焦点和减少动态适配，非交互卡片不浮动。无需加载review-animations工作流。

后续交付说明：用户已确认实现并授权合并推送；验证脚本与结果归档主目录`.deploy_state/september-screen-20260917/prototype/`（原`tmp/september-prototype/`），嘉树已替换为用户提供的原版LOGO，原型截图同步更新。上述内容保留设计阶段验证记录，实际实现验证见设计方案文末。
