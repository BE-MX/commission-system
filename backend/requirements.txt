fastapi==0.115.*
uvicorn[standard]==0.34.*
sqlalchemy==2.0.*
pymysql==1.1.*
alembic==1.15.*
pydantic-settings==2.8.*
jsonschema==4.26.*
python-dotenv==1.1.*
# Windows 不自带 IANA zoneinfo 数据，保证 Asia/Shanghai 在生产可用
tzdata>=2025.2
pyyaml==6.0.*
openpyxl==3.1.*
xlrd==2.0.1          # 读旧版 .xls（HR 工资表/社保/公积金原件），openpyxl 不支持
pypinyin==0.55.0     # 档案初始化脚本按姓名拼音生成平台用户名（scripts/salary/bootstrap）
python-multipart==0.0.*
httpx[socks]==0.28.*   # socks 扩展：展会云机生图走 SSH 隧道代理（AI_IMAGE_PROXY）
pytest==8.*
pytest-asyncio>=1.0
python-jose[cryptography]==3.3.*
passlib[bcrypt]==1.7.*
bcrypt==4.*
jinja2==3.1.*
apscheduler>=3.10.0
# 发色数字化体系依赖
colour-science>=0.4.4
numpy>=2.0
scipy>=1.13
scikit-learn>=1.4
opencv-python>=4.9
Pillow>=10.0
# 生产报工二维码
qrcode[pil]>=7.4
# AI key encryption
cryptography>=42.0
# 报表 Word 导出
python-docx>=1.1
python-pptx>=1.0
pypdf>=5.0
# AI 生图刀版文件安全预览（PDFium / resvg 均提供 Windows/Linux/macOS 预编译轮子）
pypdfium2>=5.12.1,<6
resvg_py==0.3.3
# MCP 网关(物流录单/查询对外 MCP 服务)
# 注意:mcp/sse-starlette 取新 patch 会把 starlette 下限抬到 >=0.47，与 fastapi 0.115 的
# starlette<0.47 硬冲突。`.*` 开放 patch 让全新解析(服务器)有自由度挑到冲突版本 →
# ResolutionImpossible。故三者全部钉死到 dev 实测兼容的确切版本(全为 py3-none-any 纯 wheel)。
starlette==0.46.2
mcp==1.28.1
sse-starlette==3.0.3
